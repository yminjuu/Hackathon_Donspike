import { css } from 'styled-components';

export const commonGraphWrapper = css`
  border-radius: 7.695px;
  opacity: var(--sds-size-stroke-border);

  background: #fff;
`;

export const commonChartTitle = css`
  color: var(--Grayscale-100, #111);

  /* Pretendard/B/30 */
  font-family: Pretendard;
  font-style: normal;
  line-height: normal;
`;
// font-size, font-weight 개별적 사용
